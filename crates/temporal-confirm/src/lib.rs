//! 画素読み取り結果の時系列確定。
//!
//! 1 フレームの読み取りは単独では誤りうる。HP・ドライブ・SA ゲージは
//! 「ラウンド内で増えない」「一定量以上でしか減らない」といった時間方向の
//! 性質を持つので、前後のフレームと突き合わせて確定させる。
//!
//! モジュール名は移設前と同じにしてある。`video-analyzer` 側が
//! `crate::temporal` として再輸出するため、呼び出し側の経路は変わらない。

pub use hud_vision::{frame_features, round_start};

pub mod temporal;
