//! リプレイの攻撃情報表示の読み取りと、その系列化。
//!
//! 画面下部の攻撃情報は技名・発生・持続・硬直差を数字で出す。桁の照合は
//! 入力履歴と同じ字形マッチャを使う。読み取った 1 フレーム分の観測を
//! 技ごとの並びへまとめるところまでを担う。
//!
//! モジュール名は移設前と同じにしてある。`video-analyzer` 側が
//! `crate::attack_info` として再輸出するため、呼び出し側の経路は変わらない。

pub use input_vision::input_history;

pub mod attack_info;
