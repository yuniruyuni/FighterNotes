apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: fighter
  annotations:
    run.googleapis.com/ingress: internal
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "2"
        run.googleapis.com/container-dependencies: '{"app":["db-tunnel"]}'
    spec:
      serviceAccountName: fighter-runtime@yuniruyuni-net.iam.gserviceaccount.com
      containerConcurrency: 80
      timeoutSeconds: 60
      containers:
        - name: app
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: "1"
              memory: "256Mi"
          # NOTE: PORT env var は Cloud Run が containerPort から自動注入するため設定禁止 (予約環境変数)
          env:
            - name: DB_APP_NAME
              value: fighter
            - name: PGUSER
              value: fighter_app
            - name: PUBLIC_BASE_URL
              value: https://fighter.yuniruyuni.net
            - name: SHARE_RESULTS_ENABLED
              value: "true"
            - name: SHARE_RETENTION_DAYS
              value: "30"
            - name: SHARE_CREATE_RATE_LIMIT_PER_MINUTE
              value: "10"
            - name: SHARE_DELETE_RATE_LIMIT_PER_MINUTE
              value: "10"
            - name: SHARE_GET_RATE_LIMIT_PER_MINUTE
              value: "120"
            - name: TRUST_CLOUDFLARE_CONNECTING_IP
              value: "true"
            - name: SHARE_ARGON2_CONCURRENCY
              value: "2"
            - name: SHARE_ARGON2_QUEUE_LIMIT
              value: "8"
            - name: SHARE_ARGON2_WAIT_MS
              value: "250"
            - name: SHARE_DAILY_CREATE_LIMIT
              value: "1000"
            - name: SHARE_ACTIVE_LIMIT
              value: "50000"
            - name: SHARE_STORAGE_LIMIT_BYTES
              value: "1073741824"
            - name: PGPOOL_MAX
              value: "5"
            - name: PG_CONNECTION_TIMEOUT_MS
              value: "5000"
            - name: PG_IDLE_TIMEOUT_MS
              value: "30000"
            - name: PG_STATEMENT_TIMEOUT_MS
              value: "15000"
            - name: PG_LOCK_TIMEOUT_MS
              value: "5000"
            - name: PG_IDLE_IN_TRANSACTION_SESSION_TIMEOUT_MS
              value: "30000"
            # NOTE: service は app user pw (DML のみ)。owner pw (fighter-db-password) は migration job 側で使う
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: latest
                  name: fighter-db-app-password
        - name: db-tunnel
          image: cloudflare/cloudflared:2026.7.3@sha256:e39ee8da81ad5e05d77f38d2f51c60ca51bf2a8450ac3abab50c17fdb91d91bf
          args:
            - access
            - tcp
            - --hostname
            - db.yuniruyuni.net
            - --url
            - 0.0.0.0:5432
          env:
            # client_id は CF-Access-Client-Id ヘッダで送る識別子で、認証には
            # client_secret が要る。Secret Manager は active version 数で課金される
            # ため、秘匿の要らない client_id は deploy 時に CI から埋め込む。
            # workload 毎の token 分離は client_secret 側で維持されている。
            # public repo なので値は commit せず GitHub secret 経由で置換する。
            - name: TUNNEL_SERVICE_TOKEN_ID
              value: CF_RUNTIME_CLIENT_ID_PLACEHOLDER
            - name: TUNNEL_SERVICE_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  key: latest
                  name: fighter-runtime-cf-db-access-client-secret
          resources:
            limits:
              cpu: "0.5"
              memory: "128Mi"
          startupProbe:
            tcpSocket:
              port: 5432
            periodSeconds: 1
            timeoutSeconds: 1
            failureThreshold: 30
