apiVersion: run.googleapis.com/v1
kind: Job
metadata:
  name: fighter-cleanup
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/container-dependencies: '{"cleanup":["db-tunnel"]}'
    spec:
      template:
        spec:
          serviceAccountName: fighter-cleanup@yuniruyuni-net.iam.gserviceaccount.com
          maxRetries: 2
          timeoutSeconds: 600
          containers:
            - name: cleanup
              image: IMAGE_PLACEHOLDER
              command:
                - /app/server
              args:
                - --batch=cleanup
              env:
                - name: DB_APP_NAME
                  value: fighter
                - name: PGUSER
                  value: fighter_app
                - name: CLEANUP_BATCH_SIZE
                  value: "500"
                - name: CLEANUP_MAX_BATCHES
                  value: "1000"
                - name: SHARE_RETENTION_DAYS
                  value: "30"
                - name: PGPOOL_MAX
                  value: "1"
                - name: PG_CONNECTION_TIMEOUT_MS
                  value: "5000"
                - name: PG_IDLE_TIMEOUT_MS
                  value: "30000"
                - name: PG_STATEMENT_TIMEOUT_MS
                  value: "30000"
                - name: PG_LOCK_TIMEOUT_MS
                  value: "5000"
                - name: PG_IDLE_IN_TRANSACTION_SESSION_TIMEOUT_MS
                  value: "30000"
                - name: DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      key: latest
                      name: fighter-db-app-password
              resources:
                limits:
                  cpu: "1"
                  memory: "256Mi"
            - name: db-tunnel
              image: cloudflare/cloudflared:2026.7.3@sha256:e39ee8da81ad5e05d77f38d2f51c60ca51bf2a8450ac3abab50c17fdb91d91bf
              args:
                - access
                - tcp
                - --hostname
                - db.yuniruyuni.net
                - --url
                - 0.0.0.0:5432
              env:
                # cloudrun.yaml と同様、client_id は deploy 時に CI から埋め込む。
                - name: TUNNEL_SERVICE_TOKEN_ID
                  value: CF_CLEANUP_CLIENT_ID_PLACEHOLDER
                - name: TUNNEL_SERVICE_TOKEN_SECRET
                  valueFrom:
                    secretKeyRef:
                      key: latest
                      name: fighter-cleanup-cf-db-access-client-secret
              resources:
                limits:
                  cpu: "0.5"
                  memory: "256Mi"
              startupProbe:
                tcpSocket:
                  port: 5432
                periodSeconds: 1
                timeoutSeconds: 1
                failureThreshold: 30
